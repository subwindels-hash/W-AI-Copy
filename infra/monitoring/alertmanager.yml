global:
  resolve_timeout: 5m
  # smtp_smarthost: 'smtp.example.com:587'
  # smtp_from: 'alerts@windels.ai'

route:
  receiver: default
  group_by: [alertname, severity]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  routes:
    - match: { severity: critical }
      receiver: critical

receivers:
  - name: default
    # webhook_configs:
    #   - url: 'https://example.com/hooks/alerts'
  - name: critical
    # email_configs:
    #   - to: 'oncall@windels.ai'
