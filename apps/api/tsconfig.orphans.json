{
  "$comment": "GENERATED by scripts/find-orphans.mjs --write, then hand-collapsed: the quarantined scaffolds are covered by the single 'src/_scaffolds/**' glob instead of one entry each. Re-collapse after any --write run. The two files still listed individually are NOT scaffolds and each awaits a decision - see docs/UNFINISHED_WORK_LIST.md Finding 1. To revive a scaffold: move it back to src/services/, fix its types, import it from real code, then re-run the script.",
  "exclude": [
    "node_modules",
    "dist",
    "tests",
    "src/_scaffolds/**",
    "src/enterprise/services/microservice.helper.ts",
    "src/http/middleware/tenantContext.ts"
  ]
}
